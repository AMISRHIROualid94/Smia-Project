spring:
  application:
    name: config-server
  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
      enabled: true
    util:
      enabled: true

server:
  port: 8071

management:
  endpoints:
    web:
      exposure:
        include: "*"